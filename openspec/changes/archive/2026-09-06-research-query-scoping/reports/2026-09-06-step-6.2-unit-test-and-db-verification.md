# Step 6.2 Report - Unit Tests and Database Verification (full change)

- Date: 2026-09-06
- Change: research-query-scoping
- Agent: Claude Code

## Commands Executed
- `pytest tests/unit/ -q` (backend, full suite)
- `npx tsc --noEmit` (frontend, full type-check)
- `npm test` (frontend, full Jest suite)
- `sqlite3 data/knowledge_base/editorial.sqlite "SELECT COUNT(*) FROM <table>;"` for `documents`, `stories`, `chapters`, `platform_versions`, `publish_records` (pre and post)
- `cat data/knowledge_base/editorial_memory/fuentes.md` (pre and post)

## Unit Test Results
- Backend full suite: **224 passed, 0 failed, 0 skipped** (true baseline
  before this change was 212 — see the step 4.2 report for the section-by-
  section +12 breakdown across sections 1-3)
- Frontend type-check (`tsc --noEmit`): clean, no errors
- Frontend Jest suite: **22 passed, 0 failed, 0 skipped** across 4 suites
  (baseline was 19; section 5 added 3 new tests to `PipelineFeed.test.tsx`,
  matching the implementation plan's predicted count exactly)

## Database State Verification
- Pre-test and post-test state identical: `documents=0`, `stories=0`,
  `chapters=0`, `platform_versions=0`, `publish_records=0`
- `data/knowledge_base/editorial_memory/fuentes.md` (real operator data —
  two configured source URLs) byte-for-byte unchanged throughout
- State restored: N/A — no mutation occurred (all backend/frontend unit
  tests are isolated from the real DB/memory files by design)

## Outcome
- Step 6.2 status: PASS
- Blocking issues: none

## Scope covered
Full `research-query-scoping` change: backend sections 1 (scraper adapters),
2 (query-aware scraper ordering), 3 (orchestrator/endpoint threading), and
frontend section 5 (Pipeline feed query input). E2E testing (task 6.3) is
reported below.

---

# Step 6.3 Report - E2E Testing (Playwright regression check)

- Date: 2026-09-06
- Change: research-query-scoping
- Agent: Claude Code
- Tooling: `@playwright/test` via `npx playwright test` — no Playwright MCP
  browser tool available in this environment (same precedent as prior
  changes).

## Test Environment
- Backend: reused the already-running `uvicorn` process on port 8000 (not
  restarted) — `--reload` means it already reflects this change's code.
- Frontend: reused the already-running `next dev` process on port 3000 (same
  process across this and the prior change's sessions) — not restarted.
- `ADMIN_PANEL_TOKEN` read from `frontend/.env.local` and exported for the
  `npx playwright test` run.
- **This environment now holds real operator data**
  (`data/knowledge_base/editorial_memory/fuentes.md`, two real source URLs)
  — unlike the prior change's E2E pass, this one had to seed/clean up
  `calendario.md` and `casos_cubiertos.md` (which did not exist) without
  touching `fuentes.md` at all, since neither existing E2E spec exercises
  "Ejecutar pipeline" or the source-URL list.

## Data Seeding
- `data/knowledge_base/editorial_memory/calendario.md` (did not exist
  before): one line, `optimal_time:tiktok=09:00`.
- `data/knowledge_base/editorial_memory/casos_cubiertos.md` (did not exist
  before): two lines (`AARO Radar Report` advanced, `CIA Reading Room File`
  discarded), matching `covered-cases.spec.ts`'s existing assumptions.
- One `Document`/`Story`/`Chapter`/4×`PlatformVersion` row set seeded
  directly against the real `editorial.sqlite`, titled "AARO 2024 Annual
  Report", all four platform versions starting `PENDING_REVIEW`.

## Test Results

Both specs passed on the first run, no fixes needed this time (the race
condition found and fixed in the prior change's `approval-to-calendar.spec.ts`
— waiting for the "Publicar en TikTok" button to disappear before navigating
— already covers the flow this change's query input doesn't touch):

```
Running 2 tests using 2 workers
  ✓ approval-to-calendar.spec.ts › approving then publishing a pending case surfaces it in the editorial calendar (784ms)
  ✓ covered-cases.spec.ts › searching and editing a covered case persists the change (798ms)
2 passed (1.3s)
```

This confirms the new "Tema (opcional)" input added to the Pipeline feed's
topbar in section 5 does not interfere with the existing approve → publish
→ calendar workflow or the covered-cases search/edit workflow — both
specs' selectors and flows are unaffected by the additive DOM change.

## Cleanup / Database State Restoration
- Deleted the seeded `Document`/`Story`/`Chapter`/`PlatformVersion` row set
  from the real `editorial.sqlite`. Verified post-cleanup counts:
  `documents=0`, `stories=0`, `chapters=0`, `platform_versions=0`,
  `publish_records=0` — matching the step 6.2 baseline above.
- Removed `calendario.md` and `casos_cubiertos.md` (didn't exist before this
  pass) — **left `fuentes.md` completely untouched**, confirmed byte-for-byte
  identical to its pre-test content (the two real operator source URLs).
- Removed `frontend/test-results/` (Playwright's local artifact output).
- Left the pre-existing backend/frontend dev server processes running
  exactly as found.

## Outcome
- Step 6.3 status: PASS
- Blocking issues: none. No new E2E spec was added for "Ejecutar pipeline"
  itself, per tasks.md 6.3's own scoping note — exercising it end-to-end
  would require live scraper/LLM calls against real API keys, which this
  environment now has configured but which is out of scope for a
  regression-only E2E pass on an unrelated pre-existing workflow.
