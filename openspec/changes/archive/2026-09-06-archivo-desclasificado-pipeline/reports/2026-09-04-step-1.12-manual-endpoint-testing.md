# Step 1.12 Report - Manual Endpoint Testing with curl

- Date: 2026-09-04
- Change: archivo-desclasificado-pipeline
- Agent: Claude Sonnet 5

## Server Startup
- Command: `python3 -m uvicorn src.editorial.presentation.app:app --host 127.0.0.1 --port 8123`
- Result: started cleanly, PID 61889, "Application startup complete."

## Endpoint Tests

### GET /health
- Command: `curl -s -i -X GET http://127.0.0.1:8123/health`
- Response: `200 OK`, body `{"status":"ok"}`
- Result: PASS — matches `test_health_check_returns_ok`

### Error case: unknown route
- Command: `curl -s -i -X GET http://127.0.0.1:8123/does-not-exist`
- Response: `404 Not Found`, body `{"detail":"Not Found"}`
- Result: PASS — FastAPI's default 404 behavior, as expected for an unregistered route

### Error case: wrong method on /health
- Command: `curl -s -i -X POST http://127.0.0.1:8123/health`
- Response: `405 Method Not Allowed`, body `{"detail":"Method Not Allowed"}`, `Allow: GET` header
- Result: PASS — confirms the route is GET-only as intended

## Cleanup
- Command: `kill <pid>`
- Result: process terminated; verified no longer running via `ps -p <pid>`
- No database involved in this phase (health check only) — no state to restore

## Outcome
- Step 1.12 status: PASS
- Blocking issues: none
