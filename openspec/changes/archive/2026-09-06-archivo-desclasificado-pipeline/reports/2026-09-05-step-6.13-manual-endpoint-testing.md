# Step 6.13 Report - Manual Endpoint Testing with curl

- Date: 2026-09-05
- Change: archivo-desclasificado-pipeline
- Agent: Claude Sonnet 5

## Setup
- Server: `python3 -m uvicorn src.editorial.presentation.app:app --host 127.0.0.1 --port 8127`, PID 26818.
- Seeded the real `data/knowledge_base/editorial_memory/casos_cubiertos.md` with two entries (this file/directory did not exist before this test).

## Endpoint Tests

### GET /cases — all entries
- Command: `curl -s -i -X GET http://127.0.0.1:8127/cases`
- Response: `200`, both seeded entries, correctly parsed (id, date, identifier, outcome, reason)
- Result: PASS

### GET /cases?q=radar — filtered
- Command: `curl -s -i -X GET "http://127.0.0.1:8127/cases?q=radar"`
- Response: `200`, only the "AARO Radar Report" entry
- Result: PASS

### PATCH /cases/1 — update reason
- Command: `curl -s -i -X PATCH http://127.0.0.1:8127/cases/1 -H "Content-Type: application/json" -d '{"reason": "corrected reason for testing"}'`
- Response: `200`, updated entry with the new reason
- Result: PASS

### GET /cases — verify update persisted and entry 2 untouched
- Command: `curl -s -i -X GET http://127.0.0.1:8127/cases`
- Response: `200`, entry 1 shows the corrected reason; entry 2 ("CIA Reading Room File") unchanged
- Result: PASS

### PATCH /cases/999 — unknown id
- Command: `curl -s -i -X PATCH http://127.0.0.1:8127/cases/999 -H "Content-Type: application/json" -d '{"reason": "n/a"}'`
- Response: `404`, `{"detail":"No case found with id '999'."}`
- Result: PASS

## Cleanup
- Command: `kill <pid>`; verified terminated via `ps -p <pid>`
- Command: `rm -rf data/knowledge_base/editorial_memory` — did not exist before this test, so full removal restores the exact pre-test state
- Verified via `find` that no `.sqlite` files or stray `data/` files remain beyond the two pre-existing, unrelated `docs_procesados/*.pdf` fixtures

## Outcome
- Step 6.13 status: PASS
- Blocking issues: none
