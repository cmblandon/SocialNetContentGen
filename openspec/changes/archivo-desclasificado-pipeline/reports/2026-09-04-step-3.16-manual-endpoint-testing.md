# Step 3.16 Report - Manual Endpoint Testing with curl

- Date: 2026-09-04
- Change: archivo-desclasificado-pipeline
- Agent: Claude Sonnet 5

## Setup
- Applied the editorial schema to the real DB file: `python3 -m alembic upgrade head` (note: must be run as `python3 -m alembic`, not bare `alembic` — the latter doesn't have the project root on `sys.path` and fails with `ModuleNotFoundError: No module named 'src'` when `env.py` imports the models; documented in README).
- Seeded two pending `PlatformVersion` rows against the real `data/knowledge_base/editorial.sqlite` via `persist_story` (one Document/Story/Chapter each), to exercise approve on one and reject on the other.
- Server: `python3 -m uvicorn src.editorial.presentation.app:app --host 127.0.0.1 --port 8124`, started cleanly (PID 92252).

## Endpoint Tests

### POST /platform-versions/{id}/approve — success
- Command: `curl -s -i -X POST http://127.0.0.1:8124/platform-versions/<id>/approve`
- Response: `200 OK`, `{"id":"<id>","status":"approved"}`
- Result: PASS

### POST /platform-versions/{id}/approve — already decided
- Command: same as above, run a second time against the now-approved id
- Response: `409 Conflict`, `{"detail":"PlatformVersion <id> was already decided (status='approved')."}`
- Result: PASS

### POST /platform-versions/{id}/reject — already decided
- Command: `curl -s -i -X POST http://127.0.0.1:8124/platform-versions/<id>/reject` against the same already-approved id
- Response: `409 Conflict` (same detail, correctly reports the actual current status)
- Result: PASS

### POST /platform-versions/{id}/approve — unknown id
- Command: `curl -s -i -X POST http://127.0.0.1:8124/platform-versions/does-not-exist/approve`
- Response: `404 Not Found`, `{"detail":"PlatformVersion not found"}`
- Result: PASS

### POST /platform-versions/{id}/reject — unknown id
- Command: `curl -s -i -X POST http://127.0.0.1:8124/platform-versions/does-not-exist/reject`
- Response: `404 Not Found`
- Result: PASS

### POST /platform-versions/{id}/reject — success (second seeded item)
- Command: `curl -s -i -X POST http://127.0.0.1:8124/platform-versions/<id2>/reject`
- Response: `200 OK`, `{"id":"<id2>","status":"rejected"}`
- Result: PASS

## Cleanup
- Command: `kill <pid>`; verified terminated via `ps -p <pid>`
- Command: `rm -f data/knowledge_base/editorial.sqlite` — the file did not exist before this test session, so full removal restores the exact pre-test state
- Verified via `find ... -name "*.sqlite"` that no `.sqlite` files remain in the project (excluding `venv/`)

## Outcome
- Step 3.16 status: PASS
- Blocking issues: none
